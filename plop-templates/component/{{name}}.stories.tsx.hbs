import type { Meta, StoryObj } from '@storybook/react'

import { {{name}} } from './'

const meta: Meta<typeof {{name}}> = {
  title: '{{storyPath ComponentType}}/{{name}}',
  component: {{name}},
  parameters: {
    layout: 'centered'
  },
  tags: ['autodocs'],
  argTypes: {

  }
}

export default meta

type Story = StoryObj<typeof meta>

export const Playground: Story = {}